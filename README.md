# 精料库 · 元件库管理系统

一个本地离线的元件库存 + 封装速查工具。用 Python + tkinter 写的，数据存 SQLite，不联网、启动快、随手查。

主要用来解决：手上散装元件太多、经常要查价格、查封装、批量改价，懒得开 Excel 的场景。

## 功能

- **元件管理**：增删改查，支持批量文本录入
- **封装速查**：关键词搜封装名，双击复制，可直接贴到嘉立创 EDA / KiCad
- **封装库管理**：封装可手动添加、编辑、删除，支持 CSV 批量导入导出
- **参数匹配**：按立创编号 / 型号 / 通用描述 / 多关键词匹配元件
- **批量改价**：一次给多个元件更新价格
- **价格校准**：列出超过 15 天没更新价格的元件
- **多主题**：柔和深灰蓝 / 纯黑 / 浅色

## 环境要求

- Python 3.10 或以上
- Windows 官方 Python 自带 tkinter，无需额外安装

本项目**只用 Python 标准库**，不需要 `pip install` 任何第三方包。

## 运行

```bash
cd material-library
python -m venv .venv
.venv\Scripts\activate
python main.py
